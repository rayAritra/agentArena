import{describe,expect,it}from"vitest";import{classifyRegime}from"../lib/analytics/market-regime";describe("market regimes",()=>{it("does not label insufficient samples",()=>expect(classifyRegime([1,2])).toBeNull());it("classifies observable returns mathematically",()=>{expect(classifyRegime([.7,.6,.8,.5,.9])).toBe("positive_trend");expect(classifyRegime([3,-3,4,-4,2])).toBe("high_volatility")})});
