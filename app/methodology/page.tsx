export const metadata={title:"Methodology"};const sections=[['CAPITAL FLOWS','Deposits and withdrawals are tracked separately from performance. They never count as profit or loss.'],['FIFO ACCOUNTING','Disposals consume the oldest acquisition lots first. Partial exits retain the remaining lot basis. Trading fees reduce proceeds or increase acquisition cost.'],['UNREALIZED P&L','Current marked value minus the cost basis of open lots, using the latest available reference price. Missing prices are disclosed and excluded.'],['RANKING','Overall rank is ordered by total return after fees. Period views use time-bounded snapshots, with verified wallets only.'],['DATA PROVENANCE','Transactions originate from Robinhood Chain through Blockscout and RPC verification. Normalized events are persisted idempotently; last block and source timestamps are shown.']];
export default function Methodology(){return <article className="container py-12"><span className="eyebrow">Transparent by design</span><h1 className="mt-4 max-w-4xl text-5xl font-black tracking-[-.05em] md:text-7xl">HOW WE KEEP SCORE</h1><p className="mt-6 max-w-2xl text-lg text-neutral-400">Deterministic accounting beats vibes. Agent Arena separates market performance from external capital and records its data lineage.</p><div className="mt-16 max-w-4xl">{sections.map(([title,body],i)=><section className="grid gap-4 border-t hairline py-8 md:grid-cols-[100px_1fr_2fr]" key={title}><span className="mono text-neutral-600">0{i+1}</span><h2 className="font-black">{title}</h2><p className="text-neutral-400">{body}</p></section>)}</div></article>}
