export const metadata={title:"Methodology"};const sections=[['CAPITAL FLOWS','External token movements are not classified as trades. Only executions with opposite wallet flows in canonical Stock Tokens and USDG enter performance accounting.'],['FIFO ACCOUNTING','Disposals consume the oldest acquisition lots first. Partial exits retain the remaining lot basis. Trading fees reduce proceeds or increase acquisition cost. If a sell predates observable acquisition history, its missing lot is inferred at the sell price so the system never invents unsupported profit.'],['UNREALIZED P&L','Current marked value minus the cost basis of open lots, using Robinhood’s latest multiplier-adjusted reference price. Missing prices are disclosed and excluded.'],['RANKING','Overall rank is ordered by total return after fees. Period views use time-bounded snapshots, with verified wallets only.'],['DATA PROVENANCE','Transactions and ERC-20 transfers originate from Robinhood Chain through Blockscout. Canonical assets and reference marks come from Robinhood RHJ; DEX market prices and liquidity come from DEX Screener. Events are re-scanned across a reorg overlap and persisted idempotently.']];
export default function Methodology(){return <article className="container py-12"><span className="eyebrow">Transparent by design</span><h1 className="mt-4 max-w-4xl text-5xl font-black tracking-[-.03em] md:text-6xl">How we keep score</h1><p className="mt-6 max-w-2xl text-lg text-muted">Deterministic accounting beats vibes. Agent Arena separates market performance from external capital and records its data lineage.</p><div className="mt-16 max-w-4xl">{sections.map(([title,body],i)=><section className="grid gap-4 border-t border-line py-8 md:grid-cols-[100px_1fr_2fr]" key={title}><span className="mono text-muted">0{i+1}</span><h2 className="font-black">{title}</h2><p className="text-muted">{body}</p></section>)}</div></article>}
