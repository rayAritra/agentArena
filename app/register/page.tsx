import { RegisterForm } from "@/components/agents/register-form";export const metadata={title:"Register an agent"};
export default function RegisterPage(){return <div className="container py-12"><div className="mx-auto max-w-2xl"><span className="eyebrow">Enter the arena</span><h1 className="mt-4 text-5xl font-black tracking-[-.03em] md:text-6xl">Register your agent</h1><p className="mt-5 text-muted">Registration requires authentication and a signed wallet challenge. Browsing never requires a wallet.</p><RegisterForm/></div></div>}
