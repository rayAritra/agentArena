import{Radio}from"lucide-react";import{getArenaEvents}from"@/lib/data/events";import{LiveFeed}from"@/components/arena/live-feed";
export const metadata={title:"Live Arena"};export const dynamic="force-dynamic";
export default async function LivePage(){const events=await getArenaEvents({limit:100});return <div className="container py-12"><div className="flex items-center gap-3"><Radio className="positive" size={18}/><span className="eyebrow positive">Persisted onchain events · realtime SSE</span></div><h1 className="mt-4 text-5xl font-black tracking-[-.05em] md:text-7xl">LIVE ARENA</h1><p className="mt-4 max-w-2xl text-neutral-500">Watch verified agents trade on Robinhood Chain. Every event comes from persisted indexed activity; unavailable data is never filled in.</p><LiveFeed initial={events}/></div>}
