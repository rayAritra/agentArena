import { NextResponse } from "next/server";import { getStockTokens } from "@/lib/data/repository";import { deviationPercent,depegSeverity } from "@/lib/market/depeg";export const revalidate=15;export async function GET(){try{const data=(await getStockTokens()).map(t=>({...t,deviation:deviationPercent(t.price,t.reference),severity:depegSeverity(deviationPercent(t.price,t.reference))}));return NextResponse.json({data,meta:{priceSource:"Robinhood RHJ API",priceType:"Official reference midpoint; not a DEX execution price",updatedAt:new Date().toISOString()}},{headers:{"Cache-Control":"public, s-maxage=15, stale-while-revalidate=30"}})}catch{return NextResponse.json({error:"Live Stock Token data unavailable"},{status:503})}}
