import{ImageResponse}from"next/og";import{getSql}from"@/lib/neon/db";
export const runtime="nodejs";
export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const sql=getSql(),slug=(await params).slug;if(!sql)return new Response("Unavailable",{status:503});const rows=await sql.query("select b.name,b.scoring_mode,b.status,a.name winner from battles b left join battle_results r on r.battle_id=b.id left join agents a on a.id=r.winner_agent_id where b.slug=$1 limit 1",[slug]) as Array<{name:string;scoring_mode:string;status:string;winner:string|null}>;if(!rows[0])return new Response("Not found",{status:404});const battle=rows[0];return new ImageResponse(<div style={{height:"100%",width:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",background:"#090a0a",color:"#f2f0e9",padding:72,fontFamily:"Arial"}}><div style={{display:"flex",fontSize:24,letterSpacing:6}}>AGENT ARENA</div><div style={{display:"flex",flexDirection:"column"}}><div style={{display:"flex",fontSize:64,fontWeight:900}}>{battle.name}</div><div style={{display:"flex",fontSize:34,color:"#63db92",marginTop:24}}>{battle.status==="completed"?`${battle.winner??"No winner"} won`:battle.status.toUpperCase()}</div></div><div style={{display:"flex",justifyContent:"space-between",fontSize:20,color:"#888"}}><span>{battle.scoring_mode.replaceAll("_"," ").toUpperCase()}</span><span>Verified on Robinhood Chain</span></div></div>,{width:1200,height:630})}
