import { ImageResponse } from "next/og";import { getAgent } from "@/lib/data/repository";import { percent,usd } from "@/lib/utils";
export const runtime="nodejs";export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const agent=await getAgent((await params).slug);if(!agent)return new Response("Not found",{status:404});return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",background:"#090a0a",color:"#f2f0e9",padding:"62px",fontFamily:"Arial"}}><div style={{display:"flex",justifyContent:"space-between",fontSize:22,fontWeight:800,letterSpacing:3}}><span>AGENT / ARENA</span><span style={{color:"#63db92"}}>● VERIFIED</span></div><div style={{display:"flex",flexDirection:"column"}}><span style={{fontSize:28,color:"#777"}}>#{agent.rank} AUTONOMOUS TRADER</span><span style={{fontSize:72,fontWeight:900,letterSpacing:-4,marginTop:12}}>{agent.name}</span><span style={{fontSize:112,fontWeight:900,color:agent.roi>=0?"#63db92":"#ff6b64",letterSpacing:-7,marginTop:35}}>{percent(agent.roi)}</span><span style={{fontSize:30,color:"#aaa"}}>{usd(agent.pnl)} TOTAL P&amp;L</span></div><div style={{display:"flex",justifyContent:"space-between",borderTop:"1px solid #333",paddingTop:24,fontSize:22}}><span>{agent.trades} TRADES · {agent.winRate}% WIN RATE</span><span>agentarena.xyz</span></div></div>,{width:1200,height:630})}
