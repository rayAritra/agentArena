import { NextResponse } from "next/server";import { nonceRequestSchema } from "@/lib/validation/agent";import { issueNonce } from "@/lib/security/wallet-verification";import { rateLimit,requestKey } from "@/lib/security/rate-limit";
export async function POST(request:Request){if(!rateLimit(`nonce:${requestKey(request)}`,5).allowed)return NextResponse.json({error:"Too many requests"},{status:429});try{const{address}=nonceRequestSchema.parse(await request.json());return NextResponse.json(await issueNonce(address),{headers:{"Cache-Control":"no-store"}})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Invalid request"},{status:400})}}
