import{getAgents}from"@/lib/data/public-api";import{getAgentIntelligence}from"@/lib/data/intelligence";import{ok}from"@/lib/api/response";export async function GET(){const agents=await getAgents();const rows=await Promise.all(agents.map(async a=>({agent:a,score:(await getAgentIntelligence(a.slug))?.score??null})));rows.sort((a,b)=>Number((b.score as Record<string,unknown>|null)?.arena_score??0)-Number((a.score as Record<string,unknown>|null)?.arena_score??0));return ok(rows,{methodology:"Arena Score v1.0.0",count:rows.length})}
