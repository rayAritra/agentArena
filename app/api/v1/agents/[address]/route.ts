import{z}from"zod";import{publicAgent}from"@/lib/data/public-api";import{fail,ok}from"@/lib/api/response";const id=z.string().regex(/^(0x[0-9a-fA-F]{40}|[a-z0-9-]{1,80})$/);export async function GET(_:Request,{params}:{params:Promise<{address:string}>}){const parsed=id.safeParse((await params).address);if(!parsed.success)return fail("INVALID_AGENT","Invalid wallet or slug");try{const data=await publicAgent(parsed.data);return data?ok(data,{source:data.kind}):fail("NOT_FOUND","Agent not found",404)}catch{return fail("UPSTREAM_UNAVAILABLE","Wallet data is temporarily unavailable",503)}}
