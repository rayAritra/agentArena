import{publicAgent}from"@/lib/data/public-api";import{fail,ok}from"@/lib/api/response";export async function GET(_:Request,{params}:{params:Promise<{address:string}>}){const data=await publicAgent((await params).address);if(!data)return fail("NOT_FOUND","Agent not found",404);return ok("recentTrades"in data?data.recentTrades:[],{partial:"apiWarnings"in data&&data.apiWarnings.length>0})}
