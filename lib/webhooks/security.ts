import"server-only";import{createCipheriv,createDecipheriv,createHash,createHmac,randomBytes}from"node:crypto";import{isIP}from"node:net";import{resolve4,resolve6}from"node:dns/promises";import{env}from"@/lib/env";const key=()=>createHash("sha256").update(env.WEBHOOK_ENCRYPTION_KEY??"").digest();function privateIp(ip:string){return /^(127\.|10\.|0\.|169\.254\.|192\.168\.|172\.(1[6-9]|2\d|3[01])\.|::1$|fc|fd|fe80)/i.test(ip)}export async function validateWebhookUrl(value:string){const url=new URL(value);if(url.protocol!=="https:")throw new Error("Webhook URL must use HTTPS");if(url.username||url.password||(url.port&&url.port!=="443"))throw new Error("Unsafe webhook URL");const ips=isIP(url.hostname)?[url.hostname]:[...await resolve4(url.hostname).catch(()=>[]),...await resolve6(url.hostname).catch(()=>[])];if(!ips.length||ips.some(privateIp))throw new Error("Webhook host resolves to a private or unavailable address");return url.toString()}export function encryptSecret(secret:string){if(!env.WEBHOOK_ENCRYPTION_KEY)throw new Error("Webhook encryption is not configured");const iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",key(),iv),body=Buffer.concat([cipher.update(secret,"utf8"),cipher.final()]);return Buffer.concat([iv,cipher.getAuthTag(),body]).toString("base64url")}export function decryptSecret(value:string){const raw=Buffer.from(value,"base64url"),iv=raw.subarray(0,12),tag=raw.subarray(12,28),body=raw.subarray(28),decipher=createDecipheriv("aes-256-gcm",key(),iv);decipher.setAuthTag(tag);return Buffer.concat([decipher.update(body),decipher.final()]).toString("utf8")}export function signWebhook(secret:string,timestamp:string,deliveryId:string,body:string){return`v1=${createHmac("sha256",secret).update(`${timestamp}.${deliveryId}.${body}`).digest("hex")}`}
