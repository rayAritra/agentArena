import"server-only";import{createHash,randomBytes,timingSafeEqual}from"node:crypto";export function issueApiKey(){const raw=`aa_live_${randomBytes(24).toString("base64url")}`;return{raw,prefix:raw.slice(0,16),hash:createHash("sha256").update(raw).digest("hex")}}export function verifyApiKey(raw:string,hash:string){const actual=Buffer.from(createHash("sha256").update(raw).digest("hex")),expected=Buffer.from(hash);return actual.length===expected.length&&timingSafeEqual(actual,expected)}
